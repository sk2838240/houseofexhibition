name: 🚀 Deploy to Hostinger
on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🚚 Sync Files
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Code
        uses: actions/checkout@v4

      - name: 🚀 Sync via SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 157.173.214.226
          username: u560553433
          password: 2YE519&5DpL49TI&
          port: 65002
          protocol: ftp
          secure: true
          local-dir: ./
          server-dir: /domains/houseofexhibitions.com/public_html/
          # Exclude internal GitHub files and Git history from uploading
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .github/workflows/**
